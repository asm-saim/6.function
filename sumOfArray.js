//sum of array:

const numbers = [3, 55, 77, 89, 33, 56, 88]

function arraySum(array) {
    let sum = 0;
    for (number of array) {
        sum = sum + number;
    }

    return sum;
}
const result = arraySum(numbers);
console.log(result)